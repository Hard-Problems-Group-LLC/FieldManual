<!-- FIELDMANUAL_MANAGED_HEADER_START -->
This project uses `FieldManual` as its lightweight repository guidance layer.
Both AI agents and human collaborators should reference
`{$FIELDMANUAL_ROOT}/workflow.md` and the repository root `AGENTS.md` before
doing substantial work.

Keep only a small amount of locked-in context active: the core workflow,
repo-specific safety limits, and the repository's knowledge-capture rules.

---

Any project-specific `AGENTS.md` content should go below this line and above
the managed Field Manual footer.
<!-- FIELDMANUAL_MANAGED_HEADER_END -->
